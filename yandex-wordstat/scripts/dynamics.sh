#!/usr/bin/env bash
# Динамика запроса по периодам. В API v4 Wordstat отдельного метода для помесячной динамики
# исторически нет в свободном доступе — обычно эмулируется несколькими отчётами с разными
# фильтрами дат, либо через Яндекс.Метрику (если есть счётчик на сайте).
# Оставлено как заготовка для расширения.
source "$(dirname "$0")/common.sh"
echo "[INFO] Отдельного метода динамики в Wordstat API v4 нет. См. README для альтернатив." >&2
